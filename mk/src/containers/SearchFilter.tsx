import { HStack, Select } from "@chakra-ui/react";

const SearchFilter = () => {
 return (
  <HStack className="!h-12 px-2 bg-gray-100">
   <Select size="xs" className="!bg-white" />
  </HStack>
 );
};

export default SearchFilter;
